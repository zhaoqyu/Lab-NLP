# Mike Data Pipeline Update - 2026-07-03 16:30

## What I Finished

- Added a reproducible AITA-to-DPO converter:
  - `value_alignment/prepare_aita_dpo.py`
  - Converts AITA seed data into `prompt`, `chosen`, `rejected` preference pairs.
  - Supports full train/eval/test split or a single inspectable batch file.

- Prepared the first HyDPO preference-pair batch:
  - `value_alignment/examples/first_batch_aita_preferences.jsonl`
  - 30 examples total:
    - `Security_personal`: 10
    - `Benevolence_caring`: 10
    - `Universalism_concern`: 10
  - Validation passed with 0 errors.

- Started the synthetic teacher-generation pipeline:
  - `value_alignment/generate_synthetic_preferences.py`
  - Supports diverse persona-conditioned generation.
  - Supports dry-run prompt job creation before spending API/server budget.
  - Stores concise public rationales rather than long hidden CoT traces.

- Added validation:
  - `value_alignment/validate_preference_pairs.py`
  - Checks nonempty fields, label format, chosen/rejected label consistency, and expected high/low stance mapping.

- Prepared KVS survey evaluation conversion:
  - `value_alignment/prepare_kvs_eval.py`
  - `value_alignment/examples/kvs_test_eval_sample.jsonl`
  - This lets Ali evaluate whether trained models report stronger/weaker target values on the survey-style KVS data.

## Commands To Reproduce

Create Mike's first preference batch:

```bash
python value_alignment/prepare_aita_dpo.py \
  --values Security_personal Benevolence_caring Universalism_concern \
  --max-per-value 10 \
  --single-file value_alignment/examples/first_batch_aita_preferences.jsonl
```

Validate the batch:

```bash
python value_alignment/validate_preference_pairs.py \
  --input value_alignment/examples/first_batch_aita_preferences.jsonl
```

Create dry-run teacher jobs:

```bash
python value_alignment/generate_synthetic_preferences.py \
  --values Security_personal Benevolence_caring Universalism_concern \
  --examples-per-value 1 \
  --personas-per-example 2 \
  --dry-run \
  --output value_alignment/examples/teacher_prompt_jobs_dryrun.jsonl
```

## What I Should Say In The Meeting

"I finished the first version of the data pipeline. We now have a reproducible script that converts AITA seed examples into DPO/HyDPO `prompt/chosen/rejected` pairs. I prepared a first validated batch of 30 examples across Security, Benevolence, and Universalism values. I also started the synthetic generation pipeline: it can create teacher-model prompt jobs with diverse personas and concise rationales, and later call an OpenAI-compatible teacher endpoint when we have API/model access. I added a validator so we can catch label mismatches before training."

## Questions For The Team

- For vdung: What exact input format does the HyPO training script expect on the cluster: plain `prompt/chosen/rejected` JSONL or chat-message format?
- For vdung: Should the first training run use the 30-example smoke-test batch, or should I generate the full 1,500-example AITA preference split first?
- For Ali: Which value names should we prioritize for July 13 evaluation plots: the three large values only, or all values with enough AITA data?
- For the mentor: Is it acceptable to use concise public rationales as CoT-style explanations, or do they expect longer teacher-generated reasoning traces?
- For the mentor: Should synthetic data be generated by a cloud teacher model, a local strong model on the cluster, or both for comparison?

## Risks / Blockers

- Teacher generation needs either an API key or a local teacher model endpoint.
- Long AITA posts can exceed context limits for small teacher/student models, so we may need truncation or summarization.
- Persona generation can introduce bias if personas are demographic stereotypes, so the current personas are value profiles instead.
- We need to confirm whether KVS is only evaluation data or also part of Hybrid-DPO training.

## Next Steps Before July 13

- Generate a larger synthetic batch once the teacher model is decided.
- Run validator on all generated pairs.
- Hand `train.jsonl` / `eval.jsonl` to vdung for the first DPO/HyPO cluster run.
- Coordinate with Ali so KVS value ratings and AITA probability-gain evaluation use the same value labels.
